
  # Design Zoom Workspace Replica

  This is a code bundle for Design Zoom Workspace Replica. The original project is available at https://www.figma.com/design/j8bfflw8goG39z4RqGmcb9/Design-Zoom-Workspace-Replica.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  